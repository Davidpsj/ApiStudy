# ============================================================
#  .gitignore — ApiStudy / MTG Scanner
#  Stack: .NET Core 8 · EF Core · PostgreSQL · Tesseract · Emgu.CV · ONNX
# ============================================================


# ── Build & compilação ───────────────────────────────────────
[Bb]in/
[Oo]bj/
[Oo]ut/
[Ll]og/
[Ll]ogs/

# Artefatos de publicação (dotnet publish)
[Pp]ublish/
publish/
PublishProfiles/

# ── Visual Studio / Rider / VS Code ─────────────────────────
.vs/
.vscode/
.idea/
*.suo
*.user
*.userosscache
*.sln.docstates
*.rsuser
*.DotSettings.user

# ── NuGet ────────────────────────────────────────────────────
# Pacotes restaurados pelo dotnet restore — não devem ir ao repo
packages/
*.nupkg
*.snupkg
project.lock.json
project.fragment.lock.json
artifacts/

# Garante que o nuget.config com credenciais privadas não suba
nuget.config

# ── Secrets & configurações sensíveis ───────────────────────
# NUNCA suba connection strings, JWT secrets ou API keys
appsettings.Development.json
appsettings.Production.json
appsettings.Staging.json
appsettings.Local.json
secrets.json
.env
.env.*
*.pfx
*.p12
*.pem
*.key
*.cer

# appsettings.json genérico: mantenha um appsettings.example.json
# no repo com valores fictícios para onboarding de novos devs.
# Descomente a linha abaixo se preferir ignorar também o base:
# appsettings.json

# ── Entity Framework Core ────────────────────────────────────
# Snapshots de migration são gerados localmente — podem ir ao repo
# mas os scripts SQL gerados são artefatos descartáveis
*.sql

# ── Tesseract OCR ────────────────────────────────────────────
# Dados de linguagem do Tesseract são grandes (>50 MB cada) e
# devem ser baixados pelo dev ou via script de setup, não versionados
tessdata/
*.traineddata

# ── ONNX / modelos de ML ─────────────────────────────────────
# Modelos binários são grandes e não devem ser versionados em Git.
# Use Git LFS ou armazene em blob storage (S3, Azure Blob, etc.)
*.onnx
*.pb
*.tflite
*.pt
*.pth
models/

# ── Emgu.CV / OpenCV ─────────────────────────────────────────
# Arquivos de cascade (detecção de face/objeto) baixados em runtime
*.xml.cascade
haarcascades/

# ── Arquivos temporários e de cache ─────────────────────────
*.tmp
*.temp
*.cache
*.bak
*.orig
*.swp
*~
thumbs.db
Thumbs.db
desktop.ini
.DS_Store
.AppleDouble
.LSOverride

# ── Cobertura de testes & relatórios ────────────────────────
TestResults/
coverage/
*.coverage
*.coveragexml
*.trx
BenchmarkDotNet.Artifacts/

# ── Docker ───────────────────────────────────────────────────
# Mantenha o Dockerfile e docker-compose.yml no repo,
# mas ignore overrides com senhas/portas locais
docker-compose.override.yml
docker-compose.local.yml
.dockerignore.local

# ── Node / front-end (scanner.html com dependências) ────────
node_modules/
dist/
.npm
.yarn-integrity
yarn-error.log
package-lock.json

# ── Uploads e imagens capturadas pela câmera ─────────────────
# Imagens enviadas pelos usuários para scan não devem ir ao repo
wwwroot/uploads/
wwwroot/scans/
uploads/
scans/

# ── Rider / ReSharper ────────────────────────────────────────
_ReSharper*/
*.ReSharper
*.dotCover

# ── Arquivos de sistema Windows ──────────────────────────────
$RECYCLE.BIN/
*.lnk